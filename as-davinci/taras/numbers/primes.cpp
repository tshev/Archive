#include <vector>
#include <iostream>
#include <unordered_map>

void primes(int n) {
    std::vector<bool> candidates(n, true);
    primes.reserve(std::sqrt(n) + 1);
    for (int i = 2; i <= n; i++) {
        if (candidates) {
                for (int j = i * i; j <= n; j += i) {

                }
        }
    }
}
